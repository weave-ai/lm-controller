package controller

import (
	"testing"
)

func Test_00100_GetParameters(t *testing.T) {
	/*
		g := NewGomegaWithT(t)
		lm := &aiv1a1.LanguageModel{}
		lm.Spec.Parameters = &apiextensionsv1.JSON{
			Raw: []byte(`{"temperature":0.7,"max_tokens":64,"top_p":1,"top_k":40}`),
		}
		params, err := lm.GetParameters()
		g.Expect(err).NotTo(HaveOccurred())
		g.Expect(params).To(HaveLen(4))
		g.Expect(params["temperature"]).To(Equal(0.7))
		g.Expect(params["max_tokens"]).To(Equal(64.0))
		g.Expect(params["top_p"]).To(Equal(1.0))
		g.Expect(params["top_k"]).To(Equal(40.0))
	*/
}
